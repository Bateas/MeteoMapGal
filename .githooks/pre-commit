#!/usr/bin/env sh
# Blocks a commit whose ADDED lines carry a secret, an internal address or a
# Telegram identifier. Enable once per clone:  git config core.hooksPath .githooks
exec bash "$(git rev-parse --show-toplevel)/tools/security-scan.sh" staged
