version: '3.8'

services:
  meteomap:
    build:
      context: .
      args:
        VITE_AEMET_API_KEY: ${VITE_AEMET_API_KEY}
    container_name: meteomap
    ports:
      - "8080:80"
    restart: unless-stopped
    # DNS resolution for upstream proxies
    dns:
      - 8.8.8.8
      - 1.1.1.1
