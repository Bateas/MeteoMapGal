# MeteoMapGal — Deploy Configuration
# Copy to deploy.env: cp deploy.env.example deploy.env

# Target server
DEPLOY_HOST=your.server.ip
DEPLOY_USER=root
DEPLOY_PORT=22

# SSH key (optional — omit to use password auth)
# SSH_KEY=~/.ssh/id_ed25519
