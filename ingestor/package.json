{
  "name": "meteo-ingestor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MeteoMapGal weather data ingestor → TimescaleDB",
  "scripts": {
    "start": "node --import tsx index.ts",
    "dev": "node --import tsx --watch index.ts",
    "api": "node --import tsx api.ts",
    "api:dev": "node --import tsx --watch api.ts"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "@types/pg": "^8.11.11",
    "@types/node": "^22.12.0"
  }
}
